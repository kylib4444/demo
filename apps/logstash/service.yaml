apiVersion: v1
kind: Service
metadata:
  name: logstash
  namespace: logging
spec:
  selector:
    app: logstash
  ports:
    - name: tcp-json
      port: 5000
      targetPort: 5000
    - name: http-json
      port: 8080
      targetPort: 8080
